<cartridges:WantedListUiFragment xmlns:cartridges="clr-namespace:Content.Client.CartridgeLoader.Cartridges"
        xmlns="https://spacestation14.io"
        xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
        Orientation="Vertical"
        VerticalExpand="True"
        HorizontalExpand="True">

    <!-- Wanted list page -->
    <BoxContainer Name="WantedPage" Orientation="Vertical" VerticalExpand="True" HorizontalExpand="True">
        <BoxContainer Orientation="Horizontal" HorizontalExpand="True">
            <LineEdit Name="SearchBar" HorizontalExpand="True" PlaceHolder="{Loc 'wanted-list-search-placeholder'}"/>
            <Button Name="RegistryNavButton" Text="{Loc 'weapon-registry-nav-button'}"/>
        </BoxContainer>

        <BoxContainer Name="MainContainer" Orientation="Horizontal" HorizontalExpand="True" VerticalExpand="True">
        <Label Name="NoRecords" Text="{Loc 'wanted-list-label-no-records'}" Align="Center" VAlign="Center" HorizontalExpand="True" FontColorOverride="DarkGray"/>

        <!-- Any attempts to set dimensions for ListContainer breaks the renderer, I have to roughly set sizes and margins in other controllers. -->
        <controls:ListContainer
            Name="RecordsList"
            HorizontalAlignment="Left"
            VerticalExpand="True"
            Visible="False"
            Toggle="True"
            Group="True"
            SetWidth="192" />

        <Label Name="RecordUnselected"
               Text="{Loc 'criminal-records-console-select-record-info'}"
               Align="Center"
               FontColorOverride="DarkGray"
               Visible="False"
               HorizontalExpand="True" />
        <BoxContainer Name="PersonContainer" Orientation="Vertical" HorizontalExpand="True" SetWidth="334" Margin="5 0 77 0">
            <BoxContainer Orientation="Horizontal" HorizontalExpand="True">
                <Label Name="PersonName" StyleClasses="LabelBig" />
                <RichTextLabel Name="PersonState" HorizontalAlignment="Right" HorizontalExpand="True" />
            </BoxContainer>
            <PanelContainer StyleClasses="LowDivider" Margin="0 5 0 5"/>
            <ScrollContainer VerticalExpand="True" HScrollEnabled="False">
                <BoxContainer Name="DataContainer" Orientation="Vertical">
                    <RichTextLabel Name="TargetAge" />
                    <RichTextLabel Name="TargetJob" />
                    <RichTextLabel Name="TargetSpecies" />
                    <RichTextLabel Name="TargetGender" />
                    <RichTextLabel Name="TargetShuttle" /> <!-- Radiant -->
                    <PanelContainer StyleClasses="LowDivider" Margin="0 5 0 5"/>
                    <RichTextLabel Name="InitiatorName" VerticalAlignment="Stretch"/>
                    <RichTextLabel Name="WantedReason" VerticalAlignment="Stretch"/>
                    <PanelContainer StyleClasses="LowDivider" Margin="0 5 0 5" />
                    <controls:TableContainer Name="HistoryTable" Columns="3" Visible="False" HorizontalAlignment="Stretch" />
                </BoxContainer>
            </ScrollContainer>
        </BoxContainer>
    </BoxContainer>
    </BoxContainer>

    <!-- OSK weapon registry page -->
    <BoxContainer Name="RegistryPage" Orientation="Vertical" Visible="False" VerticalExpand="True" HorizontalExpand="True">
        <BoxContainer Orientation="Horizontal">
            <Button Name="RegistryBackButton" Text="{Loc 'weapon-registry-back'}"/>
            <Label Text="{Loc 'weapon-registry-title'}" StyleClasses="LabelBig" HorizontalExpand="True" HorizontalAlignment="Center"/>
        </BoxContainer>
        <LineEdit Name="RegistrySearchBar" HorizontalExpand="True" PlaceHolder="{Loc 'weapon-registry-search-placeholder'}"/>
        <BoxContainer Orientation="Horizontal" HorizontalExpand="True" VerticalExpand="True">
            <Label Name="NoRegistryEntries" Text="{Loc 'weapon-registry-no-entries'}" Visible="False" Align="Center" VAlign="Center" HorizontalExpand="True" FontColorOverride="DarkGray"/>
            <Label Name="NoRegistryMatches" Text="{Loc 'weapon-registry-no-matches'}" Visible="False" Align="Center" VAlign="Center" HorizontalExpand="True" FontColorOverride="DarkGray"/>
            <controls:ListContainer
                Name="RegistryList"
                HorizontalAlignment="Left"
                VerticalExpand="True"
                Visible="False"
                Toggle="True"
                Group="True"
                SetWidth="192" />
            <BoxContainer Name="RegistryDetail" Orientation="Vertical" Visible="False" HorizontalExpand="True" Margin="8 0 0 0">
                <RichTextLabel Name="DetailWeapon" />
                <RichTextLabel Name="DetailSerial" />
                <RichTextLabel Name="DetailRarity" />
                <PanelContainer StyleClasses="LowDivider" Margin="0 5 0 5"/>
                <RichTextLabel Name="DetailOwner" />
                <LineEdit Name="OwnerEdit" PlaceHolder="{Loc 'weapon-registry-owner-placeholder'}"/>
                <Button Name="SaveOwnerButton" Text="{Loc 'weapon-registry-owner-save'}"/>
            </BoxContainer>
        </BoxContainer>
    </BoxContainer>
</cartridges:WantedListUiFragment>
